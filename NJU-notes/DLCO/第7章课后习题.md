# 2.
(4)
在寄存器中：寄存器直接寻址。（立即寻址的操作数在指令字中，存放在指令寄存器IR里）
在存储器中：寄存器间接寻址、直接寻址、间接寻址、基址寻址、变址寻址、相对寻址。
(9)
区别：转移跳转：直接改变PC，不保留现状；调用指令：在改变PC前，必须先将当前指令的下一条地址（返回地址）压入堆栈保存，用于后续回跳。
返回指令通常不需要地址码字段，因为它隐式地从堆栈顶弹出地址并装入PC。

