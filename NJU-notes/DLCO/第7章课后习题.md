# 2.
(4)
在寄存器中：寄存器直接寻址。（立即寻址的操作数在指令字中，存放在指令寄存器IR里）
在存储器中：寄存器间接寻址、直接寻址、间接寻址、基址寻址、变址寻址、相对寻址。
(9)
区别：转移跳转：直接改变PC，不保留现状；调用指令：在改变PC前，会先将当前指令的下一条地址（返回地址）压入堆栈保存或者保存到特定的寄存器中，用于后续回跳。
若返回字段存放在栈中或特定的寄存器中，则返回指令可以不写地址码；若返回字段存放在某个通用寄存器，则返回指令需要给出该通用寄存器的编码，即地址码。
# 3.
当这条指令被 CPU “取完”并准备执行时，PC的值已经指向了下一条指令的地址，即260，故偏移量为-40，用补码表示为1101 1000B=D8H.
# 6
设单地址指令有$k_{1}$条，则$((16-k_{2})\times 2^6-k_{1})\times 2^6\ge k_{0}$, 解得$k_{1}$的最大值为$(16-k_{2})\times 2^6-\frac{k_{0}}{2^6}$
# 7
分析：至多支持64种操作，$2^6=64$，故操作码占6位。每个操作数支持4种寻址方式，故每个操作数需要2位来标识寻址方式。一共有8个寄存器，故每个寄存器编号占3位。题目给出了存储器地址位数和立即数均为 16 位。

# 8
# 10
# 11
# 13
# 15
# 17



