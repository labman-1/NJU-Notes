**场景**：地图导航，从A到B怎么走最快？
## Floyd算法
- **“上帝视角的中转站”
- **一句话原理**：我想从i到j，能不能通过中间点k中转一下，使路径变短？（i->k->j比i->j短吗？）
- **特征：
	- **核心：3个for循环
	- **适用**：点很少（N<=500），求**任意两点**间的最短路径。
	- **记忆点**：k（中转点）在最外层循环。

## Dijkstra算法
- **“贪心的探险家”
- **一句话原理**：永远只挑**当前离起点最近**的那个点探索，永不回头。
- **核心关联：BFS plus版**
	- BFS用`queue`（FIFO）
	- Dijkstra用`priority_queue`（谁距离短谁先走）
- **适用**：点很多（N<=1e5），没有负权边，求**单源**最短路径。
