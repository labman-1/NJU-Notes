# BST
- 所有的左孩子都小于父节点，右孩子都大于父节点。
- 一定是二叉树
- spinly versus bushy
# 2-3 Tree
- 不添加叶子节点
- 每个节点最多有两个item(L=2)，超过L时会向上split
- All leaves must be the same distance from the source
- A non-leaf node with k itme