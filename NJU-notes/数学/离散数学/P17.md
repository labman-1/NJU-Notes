# 1
证明：
设简单二部图 $G = (V, E)$ 的顶点集 $V$ 可以划分为两个互不相交的独立集 $V_1$ 和 $V_2$，即 $V = V_1 \cup V_2$ 且 $V_1 \cap V_2 = \emptyset$。

设两个独立集的顶点数分别为 $v_1 = |V_1|$ 和 $v_2 = |V_2|$。根据定义，有：
$v_1 + v_2 = |V|$

因为 $G$ 是简单二部图，所以图中的每一条边都必须连接 $V_1$ 中的一个顶点与 $V_2$ 中的一个顶点，且图中不存在重边和自环。

由于 $V_1$ 与 $V_2$ 之间可能存在的最大边数在完全二部图 $K_{v_1, v_2}$ 时达到，此时每一对跨越独立集的顶点都由一条边连接。因此，任意简单二部图 $G$ 的边数 $E$ 必须满足：$E \le v_1 v_2$

根据基本不等式，对于非负实数 $v_1$ 和 $v_2$，有：
$v_1 v_2 \le \left(\frac{v_1 + v_2}{2}\right)^2$

将 $v_1 + v_2 = V$ 代入上式中，可得：
$v_1 v_2 \le \left(\frac{V}{2}\right)^2 = \frac{V^2}{4}$

因此，我们有：
$E \le v_1 v_2 \le \frac{V^2}{4}$

证毕。$\quad\blacksquare$

---

# 2
你的回答完全正确。

加入的 $n$ 条边要形成完美匹配，就必须两两不共享顶点，也就是说它们互不相邻。因此，加边方案的总数就等同于在有标号的 $2n$ 个顶点中，两两配对成 $n$ 条不相邻边的方案数，其结果正是双阶乘 $(2n-1)!!$。

下面为你提供 Problem 2 的完整推导和书写步骤。

---

### Problem 2 解答

#### 解答

要在 $2n$ 个孤立顶点中加入 $n$ 条边，使得到的图包含这 $2n$ 个顶点的完美匹配，这意味着新加入的这 $n$ 条边之间必须互不相邻（即任何两条边不共享端点）。

因此，问题转化为：**在 $2n$ 个有标记的顶点中，进行两两无序配对，共有多少种不同的配对方案？**

我们可以通过以下两种方法进行推导：

##### 方法一：分步配对法
1. 任意选择一个尚未配对的顶点，此时还剩下 $2n-1$ 个未配对的顶点，因此为该顶点选择配对对象有 $2n-1$ 种选择。
2. 在剩下的 $2n-2$ 个未配对顶点中，再任意选择一个，此时还剩下 $2n-3$ 个未配对的顶点，因此选择配对对象有 $2n-3$ 种选择。
3. 依此类推，每次配对都会使未配对顶点数减少 2。
4. 最终，总的配对方案数为：
   $(2n-1) \times (2n-3) \times \dots \times 3 \times 1 = (2n-1)!!$

##### 方法二：排列除重法
1. 将 $2n$ 个顶点进行全排列，共有 $(2n)!$ 种排列方式。
2. 我们将排列中相邻的两个顶点两两配对，形成 $n$ 个无序对：
   $(v_1, v_2), \; (v_3, v_4), \; \dots, \; (v_{2n-1}, v_{2n})$
3. 由于每一个配对内部两个顶点的顺序不影响配对结果，需要除以 $2^n$。
4. 由于这 $n$ 个配对之间的先后顺序不影响最终的匹配方案，需要除以 $n!$。
5. 因此，总的方案数为：
   $\frac{(2n)!}{2^n \times n!} = (2n-1)!!$

综上所述，总共能得到 $(2n-1)!!$ 种不同的完美匹配。 $\quad\blacksquare$

---

### 关于 Problem 3 的思考引导

我们顺利解决了前两道题。接下来是 Problem 3：
利用交错路径和可增广交错路径寻找最大匹配。

我们先来明确这两个核心概念：
- **交错路径（Alternating path）**：一条路径上的边在匹配 $M$ 中的边（粗线）和不在 $M$ 中的边（细线）之间交替出现。
- **可增广交错路径（Augmentable path）**：一条交错路径，且它的起点和终点都是未被匹配 $M$ 覆盖的孤立顶点（即自由顶点）。

观察图中的粗线（当前匹配 $M$）：
- 粗线边有：$(a_0, b_0)$, $(a_2, b_1)$, $(a_3, b_2)$, $(a_5, b_3)$。
- 此时，左侧的未匹配（自由）顶点有：$a_1$, $a_4$。
- 右侧的未匹配（自由）顶点有：$b_4$, $b_5$。

基于这些已知条件，请你试着思考并找出：
- 任意一条以自由顶点（比如从 $a_1$ 出发）开始、以自由顶点（如 $b_4$ 或 $b_5$）结束的**可增广交错路径**。它的边是如何在“细、粗、细、粗……”之间交替的？
- 一旦我们找到了这样一条可增广交错路径，应该如何对这条路径上的边进行“状态取反”（把粗线变细线，细线变粗线）来让匹配数增加 1？

你可以试着写出一条你找出的可增广路径。

# 3

# 4

