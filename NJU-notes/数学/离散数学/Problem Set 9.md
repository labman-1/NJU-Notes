# P1
(a)记A为正奇数集合。奠基：$1\in A$. 递归步骤：$\forall x\in A,x+2\in A$
(b)记B为整系数多项式集合。基础步骤：$1\in B$. 递归步骤：$\forall a,b\in B,a+b\in B,a-b\in B;\forall a\in b,a*x\in b$
(c)记C为3的正整数次幂的集合。基础步骤：$3\in C$. 递归步骤：$\forall x\in C,3x\in C$
# P2 
证明：
令n=m-1，则$n^3+(n+1)^3+(n+2)^3=(m-1)^3+m^3+(m+1)^3=3m^3+6m$
下面讨论m对3取余的情况。
若$m\equiv 0(mod 3)$,则m为3的倍数，上式可变为$3m(m^2+2)$，因为3m为9的倍数，所以上式可被9整除；若$m\equiv 1(mod 3)$,则$m^3+2m\equiv 0(mod 3)$,则$m^3+2m$为3的倍数，又因为上式可化为$3(m^3+2m)$，故上式可被9整除；若$m\equiv 2(mod 3)$,则$m^3+2m\equiv (2)^3+2*2\equiv 0(mod 3)$,则$m^3+2m$为3的倍数，又因为上式可化为$3(m^3+2m)$，故上式可被9整除. 由上三种情况知，无论 m≡0,1,2(mod3)，总有 m3+2m≡0(mod3)， 故 3(m3+2m)≡0(mod9)。
综上，当n为非负整数时，$n^3+(n+1)^3+(n+2)^3$可被9整除。
# P3
证明：命题：平面上过同一点的 n 条直线将平面分为 2n 个区域。
我们用数学归纳法证明该命题。
 **（1）基例：n=1**

当只有一条直线时，它把平面分成 2 个区域，而2=2⋅1，命题成立。

 **（2）归纳假设**

假设当有 k 条直线都经过同一点 P 时，它们将平面分成2k个区域。

 **（3）归纳步骤**
现在加入第 k+1 条直线 L，并且它也经过点 P。
由于所有直线都经过同一点 P，因此：
- 新直线 L 与已有的 k 条直线**只有一个交点**（即 P）
    
- 不会产生新的交点
    
- 原来的区域结构是由 k 条直线形成的 2k 个扇形区域
    

因此，新直线 L 只会把原来的两个扇形区域各切成两半，从而：

- 每个被切开的区域增加 1 个新区域
    
- 总共增加 2 个区域
    
于是加入 L 后的区域总数为：2k+2=2(k+1)，这正是命题在 n=k+1 时的结论。
**（4）结论**
由数学归纳法，命题对所有正整数 n 都成立。
# P4
1. 事实 1： 在任意一个拆分m=a1+a2+⋯+ak中，每一项 ai 都是正整数，且必有ai≤m，否则和就会超过 m。
2. 事实 2： 当 n≥m 时，任何满足 ai≤m 的正整数，自动满足 ai≤n。
3.  所以“用不超过 n 的正整数拆分 m”和“任意拆分 m”描述的是同一批拆分， 因此$P_{m,n}=P_m(n≥m)$.
(b)证明：
当n≥m时：由 (a) 已证，$P_{m,n}=P_m=P_{m,m}$ .
特别地，当m=1时，$P_{1,n}=P_1=1$。
当n=1时：$P_{m,1}$ 表示用不超过 1 的正整数拆分 m 的方法数。 由于 1 是唯一不超过 1 的正整数，所以只能用m=1+1+⋯+1一种方式拆分，故$P_{m,1}=1$.
当m=n>1时:此时 $P_{m,m}=P_m$，有一种拆分是使用一个m，其他所有拆分都不使用 m，即所有项都 ≤m−1， 这类拆分的个数是$P_{m,m−1}$。 因此
$P_{m,m}=1+P_{m,m−1}$.
当 m>n>1时：任何一个用不超过 n 的正整数拆分 m 的方式，要么不包含 n，要么至少包含一个 n；这两类拆分不重不漏。 不包含 n 的拆分，等价于“用不超过 n−1 的正整数拆分m”，即$P_{m,n−1}$； 至少包含一个 n 的拆分，先拿走一个 n，剩下的是“用不超过 n 的正整数拆分 m−n”，即 $P_{m−n,n}$。 因此$P_{m,n}=P_{m,n−1}+P_{m−n,n}$.
(c)
$P_5=P_{5,5}=1+P_{5,4}=1+P_{5,3}+P_{1,4}=1+(P_{5,2}+P_{2,3})+P_{1,1}=2+P_{5,1}+P_{3,2}+P_{2,2}=3+P_{1,2}+P_{3,1}+1+P_{2,1}=5+P_{1,1}+1=7$
$P_6=P_{6,6}=1+P_{6,5}=1+P_{1,5}+P_{6,4}=2+P_{2,4}+P_{6,3}=2+P_{2,2}+P_{3,3}+P_{6,2}=4+P_{2,1}+P_{3,2}+P_{4,2}+P_{6,1}=5+P_{1,2}+P_{3,1}+P_{2,2}+P_{4,1}+1=10+P_{2,1}=11$
# P5
## 1.
1. 将建筑物按位置从小到大排序。
2. 找到最左边且未被覆盖的建筑物 $x_{first}$。
3. 在 $x_{first} + 1$ 的位置放置一座基站塔（这样能保证覆盖到 $x_{first}$，且向右延伸得最远）。
4. 移除所有已被这座新塔覆盖的建筑物。
5. 重复上述步骤直到所有建筑物被覆盖。

## 2.
命题 $P(k)$：如果一组建筑物最少可以用 $k$ 座塔覆盖，那么贪心算法也一定能用 $k$ 座塔覆盖它们。
1. 奠基步：$k=1$
如果一组建筑物最少可以用 1 座塔覆盖，说明这组建筑物中最左边 $x_{min}$ 和最右边 $x_{max}$ 的距离 $|x_{max} - x_{min}| \le 2$。
按照贪心算法，第一座塔放在 $g_1 = x_{min} + 1$处。
因为 $x_{max} \le x_{min} + 2$，所以 $x_{max} \le g_1 + 1$。
这意味着所有的建筑物都在 $[g_1-1, g_1+1]$ 范围内，贪心算法也只用了 1 座塔。$P(1)$ 成立。
2. 归纳假设 
假设对于任何最少只需 $k$ 座塔覆盖的建筑物集合，贪心算法也只需 $k$ 座。
3. 归纳步骤
考虑一个最少需要 $k+1$ 座塔覆盖的集合 $X$。
设某种最优解放置的塔为 $\{o_1, o_2, \dots, o_{k+1}\}$，其中 $o_1$ 是负责覆盖最左侧建筑物 $x_{first}$ 的那座塔。
易知最优解的 $o_1$ 必须满足 $o_1 \le x_{first} + 1$(否则$o_1$无法覆盖第一座建筑物)。
而贪心算法放的第一座塔 $g_1 = x_{first} + 1$，所以 $o_1 \le g_1$。
则被 $o_1$ 覆盖的建筑物集合 $S_o$ 必然是被 $g_1$ 覆盖的集合 $S_g$ 的子集（即 $S_o \subseteq S_g$）。
这意味着，当我们用掉贪心算法的第一座塔后，剩下未覆盖的建筑物集合 $X' = X \setminus S_g$。
因为 $X \setminus S_g \subseteq X \setminus S_o$，而 $X \setminus S_o$ 显然可以被剩下的 $k$ 座最优塔 $\{o_2, \dots, o_{k+1}\}$ 覆盖。
所以，$X'$ 这个集合最少只需要 $k$ 座（甚至更少）塔就能覆盖。
根据归纳假设，贪心算法处理 $X'$ 集合也只需要 $k$ 座塔。
加上贪心算法第一步用掉的 1 座塔，总数为 $1 + k$。
因此，$P(k+1)$ 成立。
4. 结论
由数学归纳法可知，对于任意 $k$，贪心算法使用的塔数均等于最优解的塔数。
# P6
证明：
## 存在性证明：
奠基：n=2. 2是素数符合题意。
强归纳假设：假设$\forall m,2\leq m< k,m要么本身就是素数, 要么可以写为 2 个或以上的素数的积.(k\in\mathbb{N}_+,k>1)$
归纳步骤：当n=k+1时,若n是素数，显然原命题成立；若n是合数，则设$n=a\cdot b(a,b\in\mathbb{N}_+,a,b>1)$.则$1<a,b<n$,由强归纳假设知a,b要么本身就是素数, 要么可以写为 2 个或以上的素数的积,则n一定可以写成数个素数的积。
由强数学归纳法知FTM成立。
## 唯一性证明：
奠基：n=2，仅有2=2一种写法，唯一。
强归纳假设：假设对于所有满足$2\leq m<n$的整数m，其素因子分解方式是唯一的。
归纳：假设n存在两种不同的素因子分解方式。
记$n=p_1p_2p_3…p_k=q_1q_2q_3…q_s(p_1\leq p_2\leq …\leq p_k,q_1\leq q_2\leq …\leq q_s)$，因为$p_1|n$，所以$p_1|q_1q_2…q_s$ ,由欧几里得引理，$\exists r,1\leq r\leq s,q_r=p_1$,可以在两侧约去$p_1$，记$n'=n/p_1$ ,则$2\leq n'<n$ ,由强归纳假设有n‘的素因子分解方式唯一，则$n=p_1\cdot n'$，素因子分解方式唯一。
由强归纳法，唯一性成立。
综上，每个大于 1 的自然数, 要么本身就是素数, 要么可以写为 2 个或以上的素数的积，并且这些素因子按固定顺序排列后, 分解的方式是唯一的.
# P7
(a)奠基：$1\equiv 1(mod 4)且1\in S$
归纳：
(b)
# P8
