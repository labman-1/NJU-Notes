# Chapter1
## R11
总时延= $\frac{L}{R_{1}}+\frac{L}{R_{2}}$
## R12
与分组交换网络相比，电路交换网络的优点是确定性好：速率恒定、没有排队、时延稳定，不会因为网络堵塞而发生丢包。

在电路交换网络中，TDM与FDM相比，存在两个优点：
-  **全数字实现**：TDM 适合全数字化处理，硬件实现简单（数字逻辑门控制时隙分发），无需 FDM 中昂贵且易产生漂移的模拟滤波器和调制解调电路。
- **无频带浪费（保护频带）**：FDM 在频段之间必须保留防护频带以防信道串扰，而 TDM 能更充分地利用信道的整体物理带宽。
## R16
时延成分：处理时延、排队时延、传输时延、传播时延。
$$d_{\text{nodal}} = d_{\text{proc}} + d_{\text{queue}} + d_{\text{trans}} + d_{\text{prop}}$$
固定的：处理时延、传输时延（$\frac{L}{R}$）、传播时延$\frac{d}{s}$。
变化的：排队时延。
## R19
### a.
假定该网络中没有其他流量，该文件传送的吞吐量是$min\{R_{1},R_{2},R_{3}\}$=500kbps
### b.
大致用时$t=\frac{4\times 10^6\times 8\,bits}{500\times 10^3\, bps}=64$秒。
### c.
新吞吐量：$\min(500, 100, 1000)\text{ kbps} = 100\text{ kbps}$。
传输时间：$\frac{32 \times 10^6 \text{ bits}}{100 \times 10^3 \text{ bps}} = 320\text{ 秒}$
## R23
5个层次及其主要任务：
- 应用层：支持网络应用，定义端系统应用程序进程之间交互的报文格式和规则。
- 运输层：负责端到端的进程间传输，利用端口号把网络层的数据分发给具体的软件进程。
- 网络层：负责将数据报从源主机路由和转发到目的主机。
- 链路层：负责相邻两个物理节点之间的局部传输。
- 物理层：负责信号电平传输。
## R24
应用层报文是应用程序直接生成并处理的数据单元。
运输层报文段是传输层为应用层报文加上端口号、序列号等控制信息之后的数据单元。
网络层数据报是网络层为报文段封装上源与目的IP地址、TTL等路由信息后的数据单元。
链路层帧是链路层为数据报封装桑相邻硬件节点的物理地址（MAC地址）、校验和后的数据单元。
## R25
路由器处理物理层、链路层、网络层。
链路器交换机处理物理层、链路层。
主机处理全部5层（物理层、链路层、网络层、运输层、应用层）。
## P8
a. 
由于电路交换需要静态独占预留带宽，所以支持的最大用户数为
$$N_{\text{circuit}} = \left\lfloor \frac{C}{r} \right\rfloor = \frac{3000\text{ kbps}}{150\text{ kbps}} = 20\text{ 个用户}$$

b.
单个用户只有 10% 的时间在传输，因此概率如下：
    $$p = 0.1$$
c.
    设总活跃用户数为随机变量 $X$，则 $X \sim B(M, p)$，其中 $M = 120, p = 0.1$，可得所求概率为
    $$P(X = n) = \binom{120}{n} (0.1)^n (0.9)^{120-n}$$
d.21 个或更多用户同时传输的概率：
$$P(X \ge 21) = 1 - \sum_{n=0}^{20} \binom{120}{n} (0.1)^n (0.9)^{120-n}$$
    
 *概率估算（下面使用正态分布近似二项分布）：*
* 期望 $\mu = M \cdot p = 120 \times 0.1 = 12$
* 方差 $\sigma^2 = M \cdot p \cdot (1-p) = 120 \times 0.1 \times 0.9 = 10.8 \implies \sigma = \sqrt{10.8} \approx 3.286$
* 进行连续性校正，$P(X \ge 20.5)$ 对应的标准正态分布变量为：
  $$Z = \frac{20.5 - 12}{3.286} \approx \frac{8.5}{3.286} \approx 2.59$$
  查标准正态分布表得：
  $$P(Z \ge 2.59) \approx 1 - \Phi(2.59) \approx 1 - 0.9952 = 0.0048 \quad (\approx 0.5\%)$$

## P25
a.
先算单向传播时延。
$$t_{prop} = \frac{d}{s} = \frac{2 \times 10^7\text{ m}}{2.5 \times 10^8\text{ m/s}} = 0.08\text{ s} = 80\text{ ms}$$
则带宽-时延积为：
    $$R \cdot t_{prop} = (2 \times 10^6\text{ bps}) \times 0.08\text{ s} = 160\,000\text{ bits}$$
b.
最大值为160 000 bits。（实际上为文件大小与带宽-时延积中的较小值）
c.
如果将链路看作一条水管，数据看成水管中的水，传输速率R即为水管的横截面积（流量速率），传播时延即为水从进入水管到流出所需的时间，那么带宽-时延积就是这根水管内部充满水时的水的总体积。它表示在任意时刻，**链路上可以容纳的“正在飞行中”的最大比特数量**。
d.
链路长度为d = 20 000km。
所以每个比特占据的宽度为$W= \frac{d}{R\cdot t_{prop}}=125 米/比特$
一个标准足球场的长度约为105米。
所以在该链路上一个比特的宽度比一个足球场的长度更长。

# Chapter2
## R6
UDP。
原因：
- 无握手时延：UDP无需建立连接，首包更快。
- 无拥塞控制限制：UDP没有重传和拥塞控制，不会因为网络抖动而“雪崩式降速”。
## R7
远程手术：控制机械臂下刀的指令不能出现数据丢失（避免切错组织），同时对时间高度敏感（避免医生操作与机械反馈割裂）
## R11
因为这些协议传输的是文本、代码、邮件等，属于**数据丢失敏感型**。TCP可以保证数据的完整性和有序性，UDP存在丢包和乱序的风险。这体现了端到端原则的妥协——传输层作为通用基础设施，为应用层屏蔽了物理网络的不可靠性。当业务需要极低延迟且能容忍丢包（如 DNS、实时音视频）时，才会选择 UDP。
## R16
1. Alice主机->Alice的邮件服务器：使用**HTTP**协议（Alice使用浏览器登录Webmail）。
2. Alice的邮件服务器->Bob的邮件服务器：使用**SMTP**协议。
3. Bob的邮件服务器->Bob的主机：使用**IMAP**协议（Bob 运行专用本地邮件客户端，通过 IMAP 协议从自己的邮件服务器下拉并同步信箱状态）。
## R18
HOL阻塞问题：
HTTP/1.1中，服务器必须严格按照接收到请求的先后发送响应，如果排在队头的响应生成缓慢，后续已经生成的响应会被卡在缓冲区无法发送，这就是HOL阻塞问题。
解决方案：
HTTP/2将所有的传输数据切分成独立的二进制帧，并给每个帧打上流标识符。客户端与服务器可以在同一个TCP连接上并行交错地传输不同流的帧。某个流的卡顿不会阻塞其他流的数据传输，接收端只需根据stream ID 在内存中重组即可。
## P4
a.请求的完整URL：http://gaia.cs.umass.edu/cs453/index.html
位置：由请求行中的相对路径 /cs453/index.html 与 Host: 首部行 gaia.cs.umass.edu 拼接而成。在请求行中知道协议为HTTP。
b. HTTP/1.1
位置：请求行尾部。
c.持续连接。
位置：请求头的尾部有`Connection:keep-alive`
d.主机的IP地址在该HTTP报文字符串中无法找到。
e.浏览器类型：Netscape 7.2（运行在 Windows NT 5.1 上）.
位置：User-Agent: 首部行。
原因：服务器可以根据客户端的浏览器类型与引擎差异，自适应返回不同的内容。
## P5
a.成功找到了。（状态码为200 OK）
回答时间为**Tue, 07 Mar 2008 12:39:45 GMT**（来自 Date: 首部行）。
b.**Sat, 10 Dec 2005 18:27:46 GMT**（来自 Last-Modified: 首部行）。
c. **3874 字节**（来自 Content-Length: 3874）。
d.




