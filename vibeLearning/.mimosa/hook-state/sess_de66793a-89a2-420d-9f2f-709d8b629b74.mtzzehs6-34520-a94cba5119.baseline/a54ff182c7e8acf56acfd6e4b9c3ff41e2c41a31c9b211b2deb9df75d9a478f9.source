# vibeLearning — 学习协作约定

> 本目录用于跨领域学习，材料来源多样（网页、PDF、PPT、txt、书本截图、代码文件、GitHub 仓库、口头概念）。
> 本文件是给 AI 助手的指令，每次会话自动加载；用户级 AGENTS.md 的全局约定（中文交流、纯文本数学公式、诚实与验证条款等）继续生效，此处不重复。

## 目录结构

- `sources/` — 原始材料（网页存档、PDF、截图、txt、克隆的仓库），保持原样不改动
- `notes/<领域>/` — 定稿笔记
- `review/<领域>.md` — SR 复习卡片（集中式，只放入库卡片）

## 背景知识：三层方案，不维护背景档案

1. **零成本层（库即档案）**：开新话题前先检索 `NJU-notes/` 下相关笔记（现有：ICS、DLCO、SICP、数据结构、计算机网络、C++ 与算法、数学、网络攻防等），据此校准讲解深度和术语习惯。
2. **粗档案层（每学期更新一次）**：
   - 大二上（2026 秋）。在读课程：（待用户补一行）
3. **按需声明层**：用户随口一句"没学过 X / 假设我只会 C"只影响当次回答，不落盘。

校准机制：默认按"该领域有入门基础但未深入"的档位讲；回答开头用一两句话说明本次假设了什么基础，方便用户发现并打断。

## 处理档位：判读 + 宣布 + 可否决

- 用户丢材料时不必声明档位。根据信号（怎么提问、追问深度、材料性质）判读并宣布"按归档处理"或"按精学处理"，附一句理由，用户可否决。
- 随时升降级：任何时候（包括事后多日）可说"把这篇升级精学"，补检验流程。
- 档位含义：
  - **归档**：只做蒸馏和结构化，不做检验。
  - **精学**：完整流程 = 讲解/蒸馏 → 用户用自己的话复述核心、或做 2-3 道检验题 → 批改（指出具体错在哪，不说空泛的"对的"）→ 定稿。

### 精学的定稿门槛（机制代替自律）

精学笔记满足以下两条才算定稿，否则停在"半成品"状态：

1. `我的复述` 栏已由用户完成（可由 AI 转写整理，但必须源于用户原话）。
2. 候选卡片已生成并经用户筛选。

## 笔记骨架（notes/ 下统一使用）

```markdown
---
状态：半成品 | 已定稿 | 待重学    （附日期）
---

## 来源
材料出处、链接、抓取/获得日期。

## 核心问题
这份材料回答什么问题（一两句）。

## 内容
蒸馏稿。【原文转述】与【AI 补充/推断】显式区分，不混排。

## 我的复述
用户自己的话。精学必填，是定稿门槛之一。

## 遗留疑问
未解决的问题；复习中暴露的理解漏洞（死卡回流到此）。

## 候选卡片
精学结束时生成的卡片草稿。用户删改后由 AI 移入 review/<领域>.md。
（源笔记不打 #flashcards 标签，此段不会进入复习队列。）
```

## 复习卡片（Obsidian Spaced Repetition 插件）

- 插件：社区插件 **Spaced Repetition**（作者 Stephen Mwangi），支持 FSRS/SM-2 算法。
- 卡片集中放 `review/<领域>.md`，文件头用嵌套标签分牌组。格式：

```markdown
#flashcards/计算机网络/传输层

- TCP 建立连接需要几次握手？::三次
- 服务端回复什么？:::SYN+ACK          （::: 为双向卡）
UDP 适用于哪些场景？
?
实时性优先且容忍丢包：视频通话、DNS、游戏同步
可靠性由 ==序列号+确认+重传== 保证      （==高亮== 为挖空）
```

- 生成流程：精学会话结束时在源笔记"候选卡片"段生成草稿 → 用户删改 → AI 把入选卡移入 `review/<领域>.md` 入库。删改权在用户，删改本身就是一次加工。
- 卡片原则：一卡一义；优先"为什么 / 何时用 / 怎么区分"型问题，少出名词解释；答案一句话装不下就拆成多行卡或拆成两张。
- 死卡处理：连错多次的卡是理解问题而非记忆问题——移回源笔记 `遗留疑问` 栏并附一句原因，状态可改"待重学"。

## 蒸馏与诚实条款（本目录场景具体化）

- 蒸馏稿必须区分【来源原文转述】与【AI 补充/推断/评价】，不混排。
- 凭记忆给出的概念性知识和转述中不确定处，标注不确定；重要概念用户要求时联网核实。
- 网页材料注明 URL 与抓取日期；无法访问的材料明说，不假装读过。
- 每次会话结束前把笔记推进到当前状态并落盘，更新状态行。

## 会话协议

1. 新材料进入 `sources/`（或给出 URL / 路径 / 仓库地址）→ 判读并宣布档位 → 处理。
2. 精学：讲解 → 检验（复述或题目）→ 批改 → 定稿 + 候选卡。
3. 归档：蒸馏 → 按骨架落盘，状态直接标"已定稿"（归档类无复述要求）。
4. 会话结束：所有产出落盘，笔记状态行更新。
